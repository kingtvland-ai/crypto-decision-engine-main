# בוטי הסימולציה — מצב נוכחי

**נכון ל:** 2026-09-05
**היקף:** ארבעת הבוטים בדף הסימולציה בלבד. הבוט האמיתי (`/api/bot`, `/api/account`, `/api/decisions`) מחוץ להיקף ונשאר מאחורי טוקן.

מסמך זה מחליף את `PLAN_ENGINE_FIXES.md`, `REPORT_SIM_BACKTEST_ISSUES.md`,
`FOUR_BOT_ALIGNMENT.md` ו-`BOTTER.md`, שנמחקו. הם תיארו מצב שכבר לא קיים, וארבעה
מסמכים שמתארים את אותה מערכת הם ארבע הזדמנויות לסתור אותה.

---

## 1. הרישום — נקודת האמת היחידה

`packages/engine/src/services/simDefaults.ts` מגדיר מהו בוט סימולציה:

```ts
SIM_BOTS = {
  intraday: { routePrefix: '/api/sim',        confidenceScale: 'score',       minConfidence: 52, maxFutures: 2 },
  legacy:   { routePrefix: '/api/legacy-sim', confidenceScale: 'score',       minConfidence: 58, maxFutures: 2 },
  pro:      { routePrefix: '/api/pro-sim',    confidenceScale: 'score',       minConfidence: 58, maxFutures: 2 },
  path:     { routePrefix: '/api/path-sim',   confidenceScale: 'probability', minConfidence: 33, maxFutures: 0 }
}
```

נגזרים ממנו: רשימת ההיתר של שומר ההרשאות, מפתחות ה-KV, לולאת הטיק, ברירות המחדל
בשני הרנטיימים, וצבירת התיק. **הוספת בוט חמישי היא רשומה אחת כאן** — ומי שלא
מטפל בו לא מתקמפל.

זה לא ניקיון. ארבעת הבאגים האחרונים היו כולם אותה תקלה — עובדה שחייבת להיות
נכונה בכמה מקומות, נכתבה ידנית בכל אחד:

| מה נשבר | כי הרשימה נכתבה ידנית ב־ |
|---|---|
| מד הסיכון החסיר את בוט 4 | 8 חישובי צבירה ב-`SimulationBot.tsx` |
| הפאנל הציג 60, המנוע חסם על 58 | fallback התצוגה מול הקונפיג |
| סף Futures 72 מול הערה שאמרה 70 | הקוד מול התיעוד שלו |
| **כל 6 ה-endpoints של בוט 4 החזירו 401** | **רשימת הפטור בשומר ההרשאות** |

---

## 2. שתי סקאלות ביטחון — לא אחת

שלושת הבוטים מבוססי-האות מדווחים **ציון** משוקלל 0-100. בוט 4 מדווח **הסתברות** —
החסם התחתון של וילסון על אחוז הפגיעה של הדלי.

הן חולקות שם, שדה קונפיג ועמודה בממשק, והן לא ניתנות להשוואה: 33 היא הסתברות
טובה וציון גרוע.

```
BOT_MIN_CONFIDENCE=60        → intraday, legacy, pro   (ציון)
BOT_PATH_MIN_CONFIDENCE      → path בלבד               (הסתברות, ברירת מחדל 33)
```

`simBotDefaults()` אוכף את ההפרדה. לפני כן ידית אחת חלה על ארבעתם, ובערך 60 היא
ביקשה מבוט 4 דלי שפוגע ב-60% מהמקרים ביעד 1.5R — כזה לא קיים, והבוט השתתק בזמן
שההגדרה נראתה סבירה לגמרי.

---

## 3. הטבלה של בוט 4

הבוט סוחר טבלת walk-forward שנבנית אופליין. **הוא לא קורא אותה מדיסק.**

```
npx tsx scripts/pathStudy.ts snapshot --from 2021-09-01 --to 2025-07-01
npx tsx scripts/pathStudy.ts build
WORKER_URL=... BOT_ADMIN_TOKEN=... npx tsx scripts/pathStudy.ts publish
```

`publish` מעלה ל-`POST /api/path-sim/table`, שמאחסן ב-KV (Firestore בפרודקשן)
ומתקין. הטעינה קורית **באתחול** ולא בטיק — `buildEvaluations` סינכרונית, וקריאת
רשת בתוכה הייתה נכשלת בשקט בכל טיק.

הקובץ המקומי `path-study/table.json` הוא תוצר בנייה בלבד. הוא בתוך `.gitignore`,
ולכן קודם לכן פשוט לא היה קיים על השרת — הבוט נפל בשקט לבנייה in-sample בכל
פריסה, ושום דבר בלוגים או בממשק לא הבחין בין השניים.

`GET /api/path-sim/table` מדווח `source`:

| ערך | משמעות |
|---|---|
| `validated` | נבדק walk-forward. ראיה שאפשר לסחור לפיה. |
| `live-in-sample` | הגיבוי. מפעיל את המכונה, לא מוכיח כלום. |
| `none` | אין טבלה — הבוט נמנע. |

ובנפרד `readiness`, כי **טבלה ריקה משתי סיבות שונות נראית זהה**:

| ערך | משמעות |
|---|---|
| `ok` | הבנייה רצה על היסטוריה אמיתית. |
| `warming-up` | כל המטבעות דולגו מחוסר נרות. מתנקה מעצמו. **לא תוצאה אסטרטגית.** |
| `no-validated-table` | רץ על הגיבוי; שום דבר לא פורסם. |

---

## 4. עומק ההיסטוריה — נגזר, לא נכתב פעמיים

בוט 4 צריך 62 ברי 4H סגורים כדי לבנות טבלה:

```
PATH_MIN_H4_BARS  = 62
MIN_PATH_CANDLES  = PATH_MIN_H4_BARS * 4 = 248
TIMEFRAME_SPECS['1h'].targetCandles = 260   ← מכסה את הצרכן התובעני ביותר
```

קודם נכתבו 244 ו-240 בנפרד, בשני קבצים. כל מטבע נכשל בבדיקה באתחול קר והטבלה
חזרה ריקה — מצב שנראה כמו אסטרטגיה שלא מצאה כלום. `simBotRegistry.test.ts` אוכף
ש-`targetCandles >= MIN_PATH_CANDLES`, כך שהעלאת הדרישה בלי העלאת המשיכה מפילה
בדיקה במקום להחזיר בשקט את החור.

---

## 5. רמת סיכון — מה היא כן עושה ומה לא

```
riskLevelSizingMultiplier:  low 0.6  ·  medium 1.0  ·  high 1.5
                            ↓
                     תקציב הכניסה בלבד
```

היא משנה **כמה**, לא **כמה פעמים**. אף סף, גייט או תנאי כניסה לא מושפעים. זו
החלטה מכוונת — ידית אחת, אפקט אחד — ולא חוסר חיבור.

---

## 6. משתני סביבה שמשפיעים על הסימולציות

| משתנה | חל על | ברירת מחדל בקוד |
|---|---|---|
| `BOT_MIN_CONFIDENCE` | הבוט האמיתי + 3 בוטי הציון | 52 / 58 / 58 |
| `BOT_PATH_MIN_CONFIDENCE` | בוט 4 בלבד | 33 |
| `BOT_MAX_OPEN_POSITIONS` | כולם | 5 |
| `BOT_POSITION_PERCENT` | כולם | 10 |
| `BOT_RISK_LEVEL` | כולם | medium |

הדפדפן לא רואה משתני סביבה. `GET /api/public/sim-defaults` מחזיר את הקונפיג
האפקטיבי כולל שכבת ה-env, וה-hook `useServerSimDefaults` קורא אותו באתחול —
אבל **הקונפיג הרץ תמיד מנצח**: בוט באמצע ריצה הקונפיג שלו עובדה, ודריסתו במה
שבוט חדש היה מתחיל ממנו גרועה יותר מהמציין הזמני שהיא מחליפה.

---

## 7. אימות

```
npx vitest run          # 442 עוברות
npx tsc -p tsconfig.app.json --noEmit
npx tsc -p tsconfig.worker.json --noEmit
npx eslint .            # 0 שגיאות
```

הבדיקות שמחזיקות את הארכיטקטורה במקומה נמצאות ב-
`src/__tests__/simBotRegistry.test.ts`:

- **A — שוויון ניתוב:** כל ארבע הקידומות נגישות ללא טוקן; שרשרת השלילות הידנית לא חזרה.
- **B — שוויון סקאלה:** רצפת ציון לא נוחתת על בוט הסתברותי.
- **C — כיסוי המשיכה:** ה-fetcher מכסה את הצרכן התובעני ביותר.
- **D — אחסון עמיד:** הטבלה מה-KV, לא מדיסק; טבלה ריקה מדווחת למה.
- **E — לולאה אחת:** ארבעת הבוטים דרך `startSimTicker`.

אף אחת מהן לא יכלה לעבור לפני השינוי — `UI_FACING_SIM_PREFIXES` ו-`SimEnvOverrides`
לא היו קיימים, כך שהן לא מתקמפלות מול הקוד הישן.

---

## 8. פריסה

ההחלטות מחושבות בשרת. שינוי מנוע דורש **גם Render וגם Netlify**.
`/api/public/sim-defaults` הוא דרך מהירה לוודא שה-worker מריץ את הקוד הנוכחי.

---

## 9. שלושת הבוטים מבוססי-האות — מה תוקן ומה עוד לא

### 9.1 המדידה עצמה הייתה שבורה

`evaluateExit` ו-`evaluateProExit` חישבו זמן החזקה כ-`Date.now() - openTimestamp`.
בפרודקשן זה נכון. **בבקטסט שמשחזר 2025 זה החזיר כשנה**, כך שכלל תקרת ההחזקה
(72 שעות ספוט) ירה בבדיקת היציאה הראשונה ואף עסקה לא הגיעה לסטופ או ליעד.

כל תוצאה של Legacy ו-Pro שנמדדה לפני התיקון מדדה אסטרטגיה של החזקת נר אחד:

```
Legacy לפני:  95.8% מהיציאות TIME_BASED   ← פיקציה
Legacy אחרי:  SL 46.7% · TRAILING 43.3% · TIME_BASED 10.0%
```

הזמן מוזרק עכשיו דרך `portfolioStats.now`; השמטתו משחזרת את התנהגות הפרודקשן
בדיוק. פונקציה טהורה לא קוראת שעון.

### 9.2 Kelly היה מצב בולע

`calculateRiskParameters` מסרב לתמחר עסקה כשהיתרון הנמדד שלילי — נכון כשלעצמו.
אבל היתרון נמדד על **כל** ההיסטוריה, ולכן ברגע שרצף גרוע דחף אותו מתחת לאפס:
אין גודל → אין עסקאות → אין נתונים חדשים → ההערכה לעולם לא משתנה.

נמדד על Legacy: **421 אותות עברו ניתוב, 373 מהם (88%) מתו בסיזינג.**

`KELLY_MEMORY_MS` (90 יום) חוסם את הזיכרון. עסקאות מתיישנות, המדגם יורד מתחת
ל-`KELLY_MIN_SAMPLE`, הסיזינג חוזר לברירת המחדל השטוחה והבוט מודד מחדש — בלי
להיכנס במכוון ליתרון שלילי ידוע. נקודת הייחוס היא **העסקה הסגורה האחרונה**, לא
שעון הקיר, כדי לא להחזיר את הבאג מ-9.1.

### 9.3 הבסיסים האמיתיים — ולמה אסור לכוונן עליהם

```
                train (60%)      test (40%)
Legacy          PF 0.805         PF 1.935
Pro             PF 0.525         PF 0.368
Intraday        PF 0.964 (חלון מלא)
```

**Legacy נע בין 0.805 ל-1.935 בין שתי מחציות של אותו חלון.** זה לא הבדל
שאסטרטגיה מסבירה — זו הדגימה שאומרת שאין לה מספיק כוח הכרעה. שישה חודשים על
שישה מטבעות לא יכולים להכריע דבר לגבי מנועים שסוגרים 30 עסקאות בחצי חלון.

תיקון 9.2 גם אינו נמדד כאן: 90 יום זיכרון על חלון של 3.6 חודשי אימון לא מיישן
כמעט כלום. התיקון נכון מבנית; הראיה שהוא עוזר עדיין לא קיימת.

**לכן לא כוונן אף פרמטר של Legacy או Pro.** זה אותו סטנדרט שהחזקנו בו את בוט 4:
מועמד שנבחר על החלון שעליו הוא נמדד הוא מותאם, לא נמדד.

### 9.4 ההיסטוריה מגיעה מארכיוני Binance, לא ממשיכה מדפדפת

הורדות ה-snapshot היו הדבר האיטי, ולכן "בוא פשוט נמדוד על חלון ארוך יותר" הפך
כל פעם לשעה של המתנה. Binance מפרסמת את אותם נרות כ-ZIP חודשי ב-
`data.binance.vision` — **בקשה אחת לכל מטבע-חודש** במקום אלפי דפים.

`scripts/bulkKlines.ts` קורא אותם. אין תלות חדשה: ארכיון עם CSV יחיד הוא כמה
שורות מול `zlib.inflateRawSync`.

**אומת מול ה-snapshot הקיים:** 4,344 מתוך 4,345 חותמות זמן תואמות, **אפס
הפרשי OHLC**. ההפרש היחיד הוא נר הגבול — הגרסה החדשה משתמשת בסוף בלעדי, שזו
הקריאה הנכונה של "עד 2025-07-01". אותה בורסה, אותם נרות, בלי צד שלישי ליישב מולו.

```
BTCUSDT 1h, שלושה חודשים:  1.2 שניות
BTCUSDT 1h, חצי שנה:       1.5 שניות
```

שני שדות הקוד שביצעו את המשיכה המדפדפת (`fetchKlines` ב-abBacktest,
`fetchKlines15m` ב-pathStudy) נמחקו.

### 9.5 מה נדרש כדי להמשיך

snapshot ארוך ורחב יותר — סדר גודל של 40+ חודשים ו-16 מטבעות — ואז
`--split train|test` שוב. אחרי 9.4 זו הורדה של דקות, לא של שעות. עד אז כל שינוי
סף או משקל בשני המנועים האלה הוא ניחוש עם מספר לידו.

`npx tsx scripts/abBacktest.ts run --engine legacy --split train`
