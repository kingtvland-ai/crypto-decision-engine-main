# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
server/.data/
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets (never commit)
.env
.env.*
!.env.example

# One-off manual reference dumps (raw exchange data pasted in for analysis),
# not part of the app — kept on disk, not tracked.
LIST.txt
binancelist.md
.env.example
decisions.baseline*.json

# A/B harness output (scripts/abBacktest.ts): candle snapshots are large and
# re-downloadable; run results are local measurements, not source.
backtest-ab/
path-study/
ASSETS/
graphify-out/
# Playwright E2E
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
